# Icons

SVG assets for the StockPulse design system. The app currently uses the Icon component catalog in `src/components/Icon.tsx` (inline paths). Replace or extend with exported Figma SVGs here; document icon names in the Icon component.
